# Microsoft app registration values
TENANT_ID=your-tenant-id
CLIENT_ID=your-application-client-id
CLIENT_SECRET=your-client-secret-value

# Agent key to mailbox map, as a single JSON string.
# Generate one long random key per agent (for example with: openssl rand -hex 24)
# then map each key to the mailbox that agent should use.
AGENT_MAILBOX_MAP='{"replace-with-reservations-agent-key":"reservations@kwantu.co.za","replace-with-kwantu-assistant-agent-key":"assistant@kwantu.co.za","replace-with-sapphire-assistant-agent-key":"assistant@sapphireglobalfs.com"}'
